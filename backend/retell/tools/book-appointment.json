{
  "name": "book_appointment",
  "description": "Book only an exact slot previously returned by check_calendar_availability, after the caller confirms the slot and all contact and appointment details are read back.",
  "method": "POST",
  "url": "https://YOUR_PUBLIC_DOMAIN/api/tools/book-appointment",
  "headers": { "X-Tool-Secret": "YOUR_RETELL_TOOL_SECRET" },
  "payload_args_only": true,
  "parameters": {
    "type": "object",
    "required": ["start_time", "customer_name", "customer_email", "customer_phone", "customer_time_zone", "appointment_type", "stock_ids"],
    "properties": {
      "start_time": { "type": "string", "description": "Exact ISO 8601 slot returned by check_calendar_availability." },
      "customer_name": { "type": "string" },
      "customer_email": { "type": "string" },
      "customer_phone": { "type": "string", "description": "Confirmed customer phone number in E.164 format, for example +12145550123." },
      "customer_time_zone": { "type": "string", "description": "Caller's IANA timezone." },
      "appointment_type": { "type": "string", "enum": ["sales_consultation", "test_drive", "trade_in_inspection", "financing_consultation"] },
      "stock_ids": { "type": "array", "items": { "type": "string" } },
      "notes": { "type": "string", "description": "Short relevant notes only; never include financial or highly sensitive data." }
    }
  }
}
