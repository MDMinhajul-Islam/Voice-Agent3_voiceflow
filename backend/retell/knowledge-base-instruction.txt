Focus retrieval on confirmed caller constraints: stock status, seats, list price, body style, fuel, drivetrain, mileage, use case, required features, and trade-off. Prefer Available records. Include In Transit only when waiting is accepted; exclude Sold and Hold. Return stock ID, identity, price, matching evidence, and trade-off. If no exact match exists, surface the conflicting constraint and nearest valid records. Never invent missing facts.
